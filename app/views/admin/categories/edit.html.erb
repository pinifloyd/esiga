<div class='row'>
  <div class='span12'>
    <%= page_header %>

    <%= horizontal_form_for @category, url: admin_category_path(@category) do |f| %>
      <%= render 'form', f: f %>
    <% end %>
  </div>
</div>
